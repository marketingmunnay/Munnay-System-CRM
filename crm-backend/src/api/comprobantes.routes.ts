import { Router } from 'express';

const router = Router();

// Aquí luego agregas tus endpoints, por ahora puede estar vacío
// router.get('/', (req, res) => res.send('Comprobantes OK'));

export default router;
