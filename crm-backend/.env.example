# Database Configuration
# For Google Cloud SQL (Production):
# DATABASE_URL="postgresql://[USER]:[PASSWORD]@[INSTANCE_CONNECTION_NAME]/[DATABASE_NAME]?host=/cloudsql/[INSTANCE_CONNECTION_NAME]"
# 
# For Google Cloud SQL via Public IP (Development/Testing):
# DATABASE_URL="postgresql://[USER]:[PASSWORD]@[PUBLIC_IP]:5432/[DATABASE_NAME]?sslmode=require"
#
# Example:
# DATABASE_URL="postgresql://postgres:your-password@34.136.111.115:5432/basemunnaycrm?sslmode=require"

DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Server Configuration
PORT=4000
NODE_ENV=development

# JWT Configuration (Add if using JWT)
# JWT_SECRET=your-jwt-secret-key
# JWT_EXPIRES_IN=7d
